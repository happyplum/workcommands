---
description: Synchronize verified, reusable project knowledge into shared docs/ and prune redundant private tool memories after major implementation, refactor, or plan completion.
subtask: false
---

You are executing the `/doc-sync` command.

Treat `$ARGUMENTS` as optional mode and filters. Supported arguments include `fix`, `range=<git-range>` such as `origin/main...HEAD`, and `carrier=<category>`. Default mode is `audit`.

# 文档同步

完成后的文档同步审计。将已验证的代码现实与项目文档比对，检测漂移与失真；分析 Serena 等工具的私有记忆，只将值得团队长期复用的项目知识落实到共享的 `docs/`。默认仅审计；修复需显式批准。

**核心原则：** 已验证的代码现实优先于过时文档。关键事实声明不仅要「最新」，还须「真实」——经代码/配置/运行验证。工具私有记忆（Serena 等，常被 gitignore）中的可复用项目知识会形成「可见性漂移」和工具负担：审计发现后，仅经验证且未过时的知识才可压缩为团队可读的 `docs/` 内容；已完整迁移且无工具专属价值的原记忆应移除。

**本命令用于完成门控期间的回看审计。** 运行时随改随同步是实现代理自身的连续义务（见 AGENTS.md「上下文维护」）；本命令不替代它，只做完成时的集中核对。

## 模式

| 模式 | 触发 | 行为 |
|------|------|------|
| `audit` | 默认 | 仅生成漂移/同步报告 |
| `fix` | 参数含 `fix`，或用户明确说「修复/应用/更新/同步」 | 审计 + 应用已验证修复 + 执行 docs 迁移与私有记忆减负 |

指定 `carrier=<类别>` 时，其他载体列为「未审计——已过滤」。

## 工作流

### 1. 建立证据基线

读取根目录 + 最近本地 `AGENTS.md`、相关 `README.md` 产品规格章节及现有 `docs/`。识别计划文件（`.omo/plans/`、`docs/plans/`、项目根 `*.plan.md`，或 AGENTS 指明的路径）。**枚举并逐项分析记忆工具目录**：`.serena/memories/`、`.cursor/`、`.github/copilot/`、`.cline*` 等存在则纳入。

> **显式声明**：读取 gitignore 的工具私有记忆目录，目的是识别应提升到团队可见载体的知识，不改变这些目录的 git 状态。

提取：必需载体、同步规则、完成门控规则、文档边界、记忆工具清单。

### 2. 确定实现面

```bash
git diff --stat <range>
git diff --name-only <range>
```

识别触及的包/模块/服务/路由、新增/移除的端点/工作流/页面。优先审计变更面——禁止扫描整个仓库。若无法确定范围（新仓库、单次提交），对所有默认载体审计当前代码状态并注明限制。

### 3. 构建载体清单

优先使用项目中命名的载体。若无命名则使用默认值：

| 类别 | 文件 | 可见性 |
|-------|-------|-------|
| `readme` | 根目录 + 最近包的 `README.md` | 团队可见（git） |
| `agents` | 根目录 + 本地 `AGENTS.md` | 团队可见（git） |
| `docs` | `docs/**`（路线图、架构、缺口、API） | 团队可见（git） |
| `plans` | 计划/规格/进度文件 | 团队可见（git） |
| `memory-private` | `.serena/memories/*.md` 等**工具私有**记忆 | **仅装该工具者可见**（常 gitignore） |

> **关键**：`memory-private` 类载体对未安装对应工具的同事不可见。其中符合迁移范围的团队应知知识应当被识别并（经验证后）落实到 `docs/`（见步骤 6、9）。

### 4. 先分类再判断

确定内容是**描述性的**（当前事实）、**规范性的**（规则）、**愿景性的**（路线图/提案），还是**历史性的**（变更日志）。仅描述性/规范性内容被现实推翻时才算漂移。

### 5. 漂移与真实性审计

对每个载体，将声明与已验证的代码/配置/计划比对。两个维度：

**漂移**（声明 vs 现实不一致）：
- 「未实现」/「未来」/「缺口」/「TODO」语句现已不成立
- 缺失的路由/端点/服务/工作流/配置面
- 过时的「已知缺口」/「限制」/「技术债」章节
- 工作流描述不再匹配执行实际
- 记忆条目编码了已推翻的结论

**真实性**（声明是否经得起验证）——按类型抽查：

| 声明类型 | 核对要求 |
|---|---|
| critical/high 漂移声明 | **必须**验证（读代码/配置/运行） |
| 架构/拓扑/接口/运行方式类声明 | **必须**验证 |
| 措辞/摘要/路线图类声明 | 可抽查 |
| 无法验证（已删除的历史功能等） | 标记并注明原因 |

标记：`verified`（已验证为真）/ `false`（已验证为假）/ `unverified`（**尝试验证但无法确认**，或非关键可豁免——非「未查」兜底）。`unverified` 须注明尝试过什么、为何无法确认。

### 6. 私有工具记忆分析与 docs 迁移

复用步骤 5 对每条记忆的核对结果：**仅 `verified` 条目进入同步候选**；`false` 条目转入记忆失效处理；`unverified` 条目不自动提升。

**团队应知判据**：若一位**未安装该记忆工具**的同事，在缺少这条知识时会做出错误决策、重复踩坑或无法独立维护该模块，则属「团队应知」；否则不迁移。

**绝不迁移到 `docs/`**：临时记忆、暂时性计划、实现过程笔记、临时调试记录、试验性记录、个人工作流偏好、用户习惯、会话特定速查、低价值重复内容，以及已被代码或事实推翻的过时记忆。这些内容不构成团队应知长期知识；已过时且可安全处理的条目在 `fix` 模式下应失效或删除，而非转写。

识别 `memory-private` 中通过判据的 `verified` 知识，检查团队可见载体是否已包含：

| 私有记忆内容 | 落实到 `docs/` | 不迁移 |
|---|---|---|
| 架构、拓扑、模块边界 | 对应架构/模块文档 | 个人工作笔记 |
| 规范、约定、禁令 | 对应规范或维护文档 | 会话特定速查 |
| 重要运行/维护描述 | 对应运行、维护或 API 文档 | 临时调试记录 |
| 防回归知识（已验证的陷阱、契约、边界） | 对应维护或防回归文档 | 已被代码/文档覆盖的冗余记忆 |

对每条通过判据的 `verified` 团队应知知识：先检查 `docs/` 是否已有准确权威落点；缺失或过时 → 标记为**高优先级 docs 同步项**。只提取经过验证的独特事实，不复制整份私有记忆、任务历史或计划原文。已被代码推翻（步骤 5 标 `false`）→ 标记记忆为待失效。

> 同步候选须经验证后才写入（→ 步骤 9 写入门控）。

### 7. 评级严重程度

| 优先级 | 定义 |
|----------|------------|
| **critical** | 错误规则/失真事实可能立即误导实现/运维 |
| **high** | 当前状态文档实质性不正确；**团队应知知识困在私有记忆** |
| **medium** | 不完整摘要、过时缺口列表、部分漂移、未抽查的真实性 |
| **low** | 措辞清理、轻微遗漏、外观问题 |

### 8. 生成同步报告

```markdown
## 范围 — 模式、git 范围、触及区域、记忆工具清单
## 漂移摘要 — Critical/High/Medium/Low 数量
## 真实性核对 — verified/false 数量（unverified 不计总数，在发现项逐条列原因）
## docs 迁移 — 私有记忆中需落实到 docs 的条目（内容→目标文档→当前状态）
## 发现 — 按文件：[优先级] 路径 | 声明类型 | 漂移/失真 | 证据 | 修复 | 状态
## 完成状态 — 文档/记忆已更新？同步已执行？需人工后续？
```

状态值：`audit-only` | `fix-ready` | `fixed` | `manual-decision-needed`。

### 9. 应用修复（仅 fix 模式）

- **漂移修复**：仅更新有证据支持的声明，保留文档意图。AGENTS 一致性 → 两个文件都更新。
- **docs 迁移（先验证后提升）**：**仅当**记忆条目在步骤 5 中标记为 `verified` 且符合步骤 6 的迁移范围时，才将压缩后的独特事实写入对应 `docs/` 文档。`unverified`/`false` 条目即使看似团队应知，也只标记为 `manual-decision-needed`，**不自动提升**——防止私有猜测污染共享文档。
- **工具记忆减负**：写入 `docs/` 并复查准确性后，若原私有记忆已无 docs 外的独特工具价值，删除原条目而非保留镜像；若必须保留工具索引，只保留指向文档的最短定位信息。不得删除未迁移的有效私人辅助记忆。
- **记忆失效**：步骤 5 标 `false` 的私有记忆 → 更新或删除；标注推翻证据。临时、暂时性、低价值、用户习惯性或过时条目不得迁移到 `docs/`。
- **反向补全（文档→记忆）**：默认关闭。仅当满足全部三条时执行：(a) 该知识会被未来检索高频命中；(b) 当前缺失会导致检索落空；(c) 知识散落在文档多处、记忆需存一条检索索引助定位。或用户在 fix 模式显式要求。

### 10. 复查

验证：已变更文档内部一致；无载体仍陈述旧事实或失真事实；已选中的团队应知知识已在 `docs/` 中落地；已迁移私有记忆已删除或缩减为最短定位信息；记忆与文档对同一事实无矛盾镜像。

## 载体检查清单

| 载体 | 检查项 |
|---------|-------|
| README/产品规格 | 已发布状态、路由/端点、服务表、命令/安装、缺口/技术债、架构 |
| AGENTS | 工作流事实、本地规则、载体列表、同步/完成规则 |
| docs/** | 路线图状态、缺口分析、需求基线、API 文档、架构、迁移 |
| 计划文件 | 复选框、证据、延迟条目、验收标准 |
| 私有记忆（Serena 等） | 是否含应落实到 `docs/` 的架构、规范、重要说明或防回归知识；结论是否当前；临时/个人/低价值内容是否未迁移；过时是否已失效；与项目文档是否矛盾 |

## 决策规则与边界情况

**优先级：** 用户指令 > 已验证代码现实 > 现有文档 > 代理推断。模糊时 → `manual-decision-needed`。

| 情况 | 操作 |
|-----------|--------|
| 私有记忆含团队应知知识，`docs/` 缺失 | high 级 docs 同步项；fix 模式下写入对应 `docs/` 文档并清理冗余来源 |
| 私有记忆与项目文档对同一事实陈述矛盾 | 以已验证代码现实为准；更新错误一方 |
| 私有记忆已被代码推翻 | 标记记忆待失效；fix 模式下更新或删除 |
| 私有记忆未验证（步骤 5 标 unverified） | 标记 `manual-decision-needed`；不自动提升 |
| 愿景性文档写成了当前事实 | 标记——清晰标注则非漂移，但读起来像当前事实则算漂移 |
| 功能在 feature flag 后 | 「已实现，由 [flag] 门控」——而非「完全发布」 |
| 代码与计划不一致 | 代码现实优先用于文档 |
| 无记忆后端 | 报告操作为待处理 |
| 无法确定 git 范围 | 审计所有默认载体，注明限制 |

## 红旗

- 「测试通过了，文档大概没问题」
- 「README 就够了」
- 「记忆以后再同步」——知识正困在私有存储，同事看不到
- 「记忆里写了就行」——同事没装该工具，记忆对他们不可见
- 「整份记忆都迁移」——会把临时计划、个人习惯和过时噪音写进共享文档
- 「差不多就行，真实性不用逐条验证」

这些意味着漂移/失真/知识孤岛正在被合理化。停下来审计。
