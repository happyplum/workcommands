---
description: 为任意项目初始化和维护功能单元计划与 shipped 事实清单，防止实现回退与规格漂移。无参数时自动判断项目场景（全新 / 部分就绪 / 完整）并提问确认后执行；也可带 check 执行健全性检查或带单元名维护清单。适用于前端页面、后端 API、库模块等任意功能单元。
---

# shipped 清单管理

管理本项目的「功能单元清单」——按功能单元（页面、API、模块等）独立记录计划与已落实的事实，防止实现回退与规格漂移。工作流：开发前用 `[pending]` 记录计划，落地后改为 `[shipped]`，修改时维护已有条目不新建。

用户输入：`$ARGUMENTS`

## 参数解析

| `$ARGUMENTS` | 行为 |
|---|---|
| 空 | **自动判断项目场景**，提问用户确认后执行（见下「场景判断」） |
| `check` | 直接执行健全性检查 |
| `<单元名>`（如 `auth-login`、`user-api`） | 直接对该单元执行 add / update（维护清单） |

未识别的输入当作单元名处理。除非用户明确指定，不要假设目录名——先用「shipped root 发现」扫描确认。

---

## 场景判断（无参数时执行）

无参数运行时，**先检测项目状态，再提问，用户确认后才执行**。不擅自修改任何文件。

### 第一步：检测状态

用「shipped root 发现」和以下检查判断项目处于哪种场景：

- shipped 清单目录是否存在？有几份清单？
- AGENTS.md 是否含 `shipped-workflow` 边界区块？
- 是否有孤儿清单（清单存在但无 AGENTS 提示词）或 AGENTS 提示词存在但无清单？

### 第二步：按场景提问

| 场景 | 判断条件 | 提问内容 | 确认后执行 |
|---|---|---|---|
| **全新项目** | 无清单目录 + 无 AGENTS 提示词 | 「检测到全新项目。是否初始化 shipped 清单框架？」 | 「初始化流程」 |
| **部分就绪** | 有清单但缺 AGENTS 提示词，或反过来 | 报告现状：「发现 N 份清单但缺 AGENTS 工作流提示词」+ 询问「补全提示词？/ 跑 check？/ 维护某个单元？」 | 对应操作 |
| **完整结构** | 有清单 + AGENTS 提示词 | 「做什么？1) check 健全性  2) 维护某个功能单元  3) 查看索引」 | 用户选择的操作 |

- 如果检测到既有结构但目录非默认（如 `docs/product/pages/`），在提问中说明发现的目录，让用户确认或修正。
- **绝不擅自覆盖**已有内容。

---

## shipped root 发现

本命令不硬编码目录。按以下顺序发现 shipped 清单根目录：

1. **读 AGENTS.md**：查找 shipped root 声明（如 shipped-workflow 边界区块中的目录路径）。
2. **读 README.md**：查找指向 shipped 清单目录的索引引用。
3. **检查候选目录**：`docs/shipped/`、`docs/product/pages/`、`docs/specs/` 等常见位置是否有含 `[shipped]` 标记的 `.md` 文件。
4. **回退默认**：以上都未找到时，默认 `docs/shipped/`。

发现到自定义目录时，场景判断 / check / add / AGENTS 注入都使用该目录。向用户报告发现的目录，让用户确认或修正。

---

## 初始化流程（全新项目场景触发）

当场景判断为「全新项目」且用户确认后执行此流程。也可由用户在部分就绪场景中显式选择。

### 前置检查

1. 确认项目根目录（`git rev-parse --show-toplevel`，或回退到当前工作目录）。
2. 如场景判断尚未检测，用「shipped root 发现」确认是否有既有结构。有则报告并询问复用还是新建，**不擅自覆盖**。

### 建立目录与文件

1. **创建目录**（用户确认的 shipped root，默认 `docs/shipped/`）。扁平结构为主；如果项目功能单元分类明确（如同时有页面和 API），询问是否需要子目录（如 `pages/`、`api/`）。

2. **生成 `<shipped-root>/README.md`**——格式约定 + 同步规则 + 索引。使用下方「README 模板」，根据项目实际情况填充索引。

3. **扫描项目结构**，为已存在的功能单元生成初始清单骨架。优先用 glob/grep 工具；检测方式因项目类型而异：
   - **Flutter**：`lib/features/*/presentation/screens/*_screen.dart`、路由定义中的路径（`GoRoute`/`path:`）
   - **Next.js / React**：`app/**/page.{js,jsx,ts,tsx}`、`pages/**/*.{js,jsx,ts,tsx}`、路由配置文件
   - **Express / Fastify / Gin / Echo / FastAPI**：路由注册（`app.get/post`、`router.Group`、`@app.get`）
   - **Go / Rust / Python CLI**：`cmd/` 子命令、`cobra`/`clap`/`click`/`typer` 命令定义
   - **通用兜底**：`src/`、`lib/` 下的一级功能目录
   - 无法自动检测时，**询问用户**列出需要记录的功能单元，不要编造。

4. **空项目处理**：如果扫描未发现任何功能单元（新项目、仅含脚手架），询问是否创建 `project.md` 作为项目级清单。

5. 初始清单只写已确认的事实（文件路径、入口），不确定的标 `[pending]`。**绝不编造**未验证的实现细节。

### 写入 AGENTS.md 提示词

1. **读取现有 AGENTS.md**（没有则创建）。
2. **检查冲突**：grep 是否已有 `shipped-workflow` 边界区块或等价的文档同步规则节。已有则报告，不重复注入。
3. **用边界区块注入**，便于后续 check 和更新识别：

~~~markdown
<!-- shipped-workflow:start -->
## shipped 清单工作流（防回退 / 漂移）

- shipped root：`<shipped-root>`（本项目的 shipped 清单目录）
- **修改 / 开发功能单元前**：先加载该单元对应的 `<shipped-root>/<unit>.md` shipped 清单，了解已落实事实和当前边界。
- **开发前记录计划**：可用 `[pending]` 记录计划开发的内容（文件路径、预期行为、接口签名）；落地后改为 `[shipped]`。
- **功能单元开发完毕后**：必须维护对应的 `<shipped-root>/<unit>.md` shipped 清单——将 `[pending]` 改为 `[shipped]`、追加新事实、修正过时标记，不新建重复条目。
- **单元级事实不入 README**：每个功能单元独立一份文件；根 README 与 `<shipped-root>/README.md` 只放索引，不放单元事实。
<!-- shipped-workflow:end -->
~~~

注：`<shipped-root>` 替换为实际目录路径。如果项目已有等价的文档同步规则节，合并而非重复。

### 完成报告

报告：shipped root 目录、创建了哪些文件、生成了几份初始清单、AGENTS.md 是否更新、建议用户下一步（补充精确事实）。

---

## 健全性检查（check）

可由场景判断触发（完整结构场景选项 1），或用户直接输入 `/shipped check` 执行。

用「shipped root 发现」确定目录。逐项执行并汇总，用 glob/grep/read 工具检查，不修改任何文件。

### 检查项

1. **断链检查**：grep 所有 `<shipped-root>/*.md` 中反引号包裹的**路径型**引用（特征：含 `/`、有文件扩展名如 `.md/.dart/.ts/.go/.py`、或以 `lib/` `src/` `app/` 开头）。用 glob 或 read 验证存在。**不把字段名、接口名、类名误报为路径**。报告不存在的路径及所在清单文件。

2. **格式合规**：每份清单是否含至少一个状态标记。报告完全没有标记的文件。

3. **索引一致性**：
   - `<shipped-root>/README.md` 索引表列出的文件是否都存在
   - `<shipped-root>/` 下的 `.md`（除 README）是否都在索引中
   - 报告「孤儿文件」（存在但未索引）和「幽灵索引」（索引但不存在）

4. **反向覆盖**：扫描代码中的功能单元（用初始化流程的框架检测逻辑），检查每个单元是否有对应的清单文件。报告「代码存在但清单缺失」的单元。这是防止漂移的关键检查。

5. **过期检测（尽力，启发式）**：
   - 对每份清单，找到其引用的主代码文件
   - 用 `git log -1 --format=%ci <file>` 比较代码和清单的最后提交时间；未纳入 git 的文件用文件系统修改时间回退
   - 用 `git status --porcelain` 检查清单引用的代码文件是否有未提交改动
   - 代码修改晚于清单、或有未提交改动时标记「可能过期」
   - **只提示不修改**，可能误报

6. **AGENTS 提示词检查**：确认 AGENTS.md 含 `shipped-workflow` 边界区块。缺失则提示用户手动加入或重新运行本命令。

### 报告格式

按 6 项逐项给 ✅ / ⚠️（列出具体问题），最后给总结（通过 / 发现 N 个问题）。

---

## 维护清单（add / update）

可由场景判断触发（完整结构场景选项 2），或用户直接输入 `/shipped <单元名>` 执行。

1. 用「shipped root 发现」确定目录。
2. 接收单元名（如 `auth-login`、`user-api`、`payment-module`）。文件名用 kebab-case。
3. **判断文件是否存在**：
   - **不存在**：用「清单文件模板」创建 `<shipped-root>/<name>.md`。
   - **已存在**：**先读取现有内容**，在其基础上维护——功能修改时不新建条目，而是追加新事实、将 `[pending]` 改为 `[shipped]`、修正过时标记。**不覆盖**已有事实，只增改。
4. 更新 `<shipped-root>/README.md` 索引表（新文件加入条目）。
5. 提示用户确认最终内容。

---

## 模板

### README 模板（<shipped-root>/README.md）

~~~markdown
# shipped 清单

本目录记录每个功能单元的**计划、设计状态与已落实事实**，防止实现回退与规格漂移。

## 格式约定

每份清单用扁平列表，标记当前状态：

| 标记 | 含义 |
|---|---|
| `[shipped]` | 已实现并验证 |
| `[tech-debt]` | 已知偏差或临时实现，待后续修正（**附加标记**，可与 `[shipped]` 同时使用，表示「已实现但有技术债」） |
| `[stub]` | 占位实现，功能不可用 |
| `[pending]` | 尚未实现 |
| `[designed]` | 规格已设计，实现待接入 |

状态升级路径：`[designed]` → `[pending]`（进入开发排期）→ `[shipped]`（实现验证）。

每条记录应包含**精确事实**：文件路径、字段名、接口、行为边界。不写泛化用户故事、不可验证愿望或大段状态机模板；`[pending]` / `[designed]` 条目必须包含可验证对象。

## 索引

| 单元 | 清单文件 |
|---|---|
| （初始化时自动填充，维护时追加） |

## 同步规则

见项目 `AGENTS.md` 的 shipped 清单工作流节。
~~~

### 清单文件模板（<shipped-root>/<name>.md）

~~~markdown
# <单元名> `<路由或入口标识>`

- [shipped] （已实现的事实：文件路径、字段、行为）
- [shipped] [tech-debt] （已实现但有技术债：附 tech-debt 标记说明偏差）
- [designed] （规格已设计，实现待接入）
- [stub] （占位实现）
- [pending] （尚未实现）
~~~

---

## 通用规则

- **先读后写**：修改任何清单前先读取现有内容，不覆盖已有事实。
- **计划与事实并存**：开发前可用 `[pending]` 记录计划开发的内容（文件路径、预期行为）；落地后改为 `[shipped]`；修改功能时维护已有条目，不新建重复条目。
- **精确路径**：每条事实尽量包含文件路径、字段名、接口签名等可验证信息。
- **原子更新**：一次开发完成后，更新对应清单并提交，不积累多处改动。
- **工具优先**：优先用 glob/grep/read/write 工具操作文件（跨平台）；需要时间戳或提交历史时可用 git 命令（`git log`、`git status`）。
- **不编造**：无法确认的实现细节标 `[pending]`，绝不猜测后写成 `[shipped]`。
