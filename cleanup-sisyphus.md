---
description: Audit and clean the .sisyphus / .omo workspace and docs/ directory, with an explicit full-purge mode that preserves current durable documentation in docs/.
subtask: false
---

You are executing the `/cleanup-sisyphus` command.

Treat `$ARGUMENTS` as the cleanup scope, target subdirectory, or any extra retention constraints.

# Sisyphus / Omo / Docs 清理

## 概述

将 `.sisyphus` / `.omo` / `docs/` 清理视为**验证优先的执行产物清理与结构整理**，而非盲目文件清除。

**删除不是默认动作。** 有用且仍准确的架构、设计决策、边界/契约、拓扑、防回归约束、运维/交接事实，必须先**落实到项目本地 `docs/`（或既有 README / ADR）**，再允许删除其 `.omo` 原件。截图、执行日志、checkbox 计划正文、会话状态默认可删；**不得**在知识未落地时「先删后补」。

**核心编排（阶段门禁，禁止跳步）**：

1. **探测** → 2. **父级只读项目列表** → 3. **子代理分析波（现实完成度）** → 4. **汇总裁决 / 是否进入整理删除** → 5. **全量分类 + 覆盖率** → 6. **知识提取并落地 `docs/`** → 7. **知识落地门禁通过后 dry-run** → 8. **一致删除** → 9. **证明结果**

禁止：父级未产出项目列表就派分析；分析波未闭合就生成 DELETE 清单或删除；「只扫已删 plan 同名附属」捷径；「open 多 / 无 boulder ⇒ 未完成」；**未完成 docs 知识落地就删含架构/设计事实的源文件**。

## 明确全量清理模式

当用户明确要求删除指定 `.omo` 目录的全部内容（例如 `F:\frontend\tnyma2.0\.omo`），以下规则优先于本文件中“未知默认保留”和“活跃根保留闭包”的常规规则：

1. **目标结果**：在完成有用且仍准确的文档提取后，删除该指定 `.omo` 目录及其全部内容。
2. **不信任 `boulder.json`**：`boulder.json` 及同类 Agent 状态文件只能作为盘点线索；其状态、引用、清单或标记均不得单独证明计划仍活跃、阻止删除或形成保留闭包。
3. **未完成计划的唯一阻断条件**：只有项目现实能证明某项计划仍未完成时才阻止删除。证明必须指出具体尚未交付的目标，并由代码、测试、配置、用户可见文档、Git 历史或当前 issue tracker 中至少一项佐证；仅有 `.omo` 内的计划文本、状态文件或 Agent 日志不足以构成证明。
4. **文档先落地**：逐项提取目标 `.omo` 中面向用户或维护者、仍准确且未被现有文档覆盖的事实，写入或更新项目 `docs/` 下主题明确的持久文档。若 `docs/` 不存在且确有此类事实，创建最小的 `docs/` 目录及单一对应文档；不得搬运执行日志、任务状态或整份计划原文。
5. **确认未完成时的处理**：停止删除该 `.omo` 目录，报告未完成目标及其外部证据；仅可删除已验证无关的临时产物，不得依据 `boulder.json` 推断额外保留范围。
6. **未证明未完成即继续清理**：无法由项目现实证明计划未完成，不是保留理由；完成文档落地、引用核验和 dry-run 后，继续删除整个指定目录。

## 精确保留算法

1. **活跃根必须由项目现实验证**：计划、运行、任务状态文件或检查点只有在 `.omo` 之外的代码、测试、配置、用户文档、Git 历史或 issue tracker 共同证明其当前未结时才可参与判定。`boulder.json` 永远不能作为活跃根或验证来源。
2. **只保留精确直接引用**：仅当条目被以下任一形式直接引用时才进入保留闭包：路径、plan/run/task/dependency ID、清单项、显式内容链接。
3. **递归闭包只跟随已证实引用**：被直接引用的条目可继续展开其自身显式引用到的依赖；仅保留这个精确引用闭包。
4. **目录不是引用，邻近关系也不是引用**：目录相邻、同级、同名、同前缀、同时间段创建，都不构成保留理由。
5. **目录只作为容器保留**：只有当目录中至少有一个被保留子项时，才保留该目录本身；空目录和无保留子项目录不保留。
6. **未知是最后一档，不是默认出口**：只有在按「分类顺序」走完（活跃闭包 → 已完成/附属 → 临时产物 → 已转换证据）之后仍无法判定时，才标「未知」并保留报告。禁止把「未链到活跃 plan」直接当成未知。
7. **已转换证据默认删除**：若证据对应的持久事实已完整落在代码、测试、文档、Git 历史、现有便签或记忆中，且不再提供新增唯一事实，则删除原件，不再二次摘要或复制事实。
8. **反向闭包必扫**：建立保留闭包后，盘点清单中**每一个**不在闭包内的路径，必须进入全局逐项处置清单（删除候选 / 未知 / 知识提取后删除）。禁止只处置「与已删 plan 同名」的附属物而漏扫其余桶。

## 编排与阶段门禁（MUST）

本节是**执行骨架**；与后文「执行顺序」「防遗漏硬门禁」冲突时，**以本节阶段门禁为准**。
`/cleanup-sisyphus` 是**多阶段编排命令**，不是「边读 plan 边删」的单循环。
**任一前序阶段未闭合 → 禁止进入后序阶段；阶段 3 未放行 → 禁止任何删除 dry-run / 删除。**

### 阶段 0 — 探测

按「目录探测」确定 `$WORKSPACE`（`.omo` / `.sisyphus`）与 `docs/`；需要时执行迁移协议准备。
本阶段只确认目标是否存在，不分类、不删除。

### 阶段 1 — 父级只读「项目列表」（轻量，MUST 先做）

父协调者 **MUST 亲自**（直接工具，不派实现 worker）读取并输出**项目列表**，至少包含：

| 列表项 | 内容 |
|---|---|
| plans | 每个 `plans/<slug>.md`：slug、路径、粗计 open/done（仅线索） |
| drafts | 每个 draft 文件名 / 是否声明 `plan_path` |
| notepads | 每个 `notepads/<slug>/` 是否存在及文件数 |
| evidence | 每个一级子目录名 + evidence 根级文件数（不深读截图内容） |
| 状态线索 | `boulder.json` 中 works 的 slug↔status 摘要（**仅线索**） |
| 可选 | 已注册 worktree 名、根孤儿文件名列表 |

**本阶段允许**：列目录、读 plan 标题/Goal 摘要、计 checkbox 数、读 boulder 摘要。
**本阶段禁止**：

- 对产品代码做深度完成度裁决（大范围 grep/codegraph 实现核对）
- 填写最终 `completed` / `unfinished` 裁决并当作定案
- 生成 DELETE 清单、dry-run、删除任何 `$WORKSPACE` / `docs` 文件
- 以「我已看过列表」跳过阶段 2

**阶段 1 出口物**：一份可粘贴的「项目列表」报告（每 plan 一行 + 各桶计数）。无此出口物不得进入阶段 2。

### 阶段 2 — 子代理分析波（现实完成度，MUST）

在项目列表就绪后，父协调者 **MUST 安排子代理** 按「防遗漏硬门禁 §3」对 plan 做**项目现实优先**完成度分析，以决定哪些 slug 可进入后续整理删除。

#### 2.1 是否强制派子代理

| 条件 | 动作 |
|---|---|
| `plans` 数量 ≥ 2 | **必须**派子代理分析；父级只汇总，**禁止**父级独自对全部 plan 做深度现实核对后跳过派发 |
| `plans` 数量 = 1 | **默认**派 1 个只读分析子代理；仅当目标明显 trivial（纯流程壳、无产品 Goal）时父级可有界自判，并在报告中声明「单 plan 父级自判」 |
| `plans` 数量 = 0 | 跳过阶段 2，直接进入阶段 3（仅附属/孤儿/docs 临时产物分类） |

#### 2.2 分片与并发

- **按 plan 分片**：每个分析 task 负责 **1 个 slug**，或 **一组明确重叠交付**的 slug（例如同一功能的 `qr-create-retry-fallback` + `qr-create-serial-restart-fix`）；禁止把无关 plan 塞进同一 task。
- **并行**：独立 plan 可并行派发；写入并发上限不适用（本波**只读**）。建议同时在飞分析 task ≤ 3，其余 pending。
- **路由**：优先 `subagent_type="explore"` 或 category `quick` / `unspecified-low` 的**只读**分析；`load_skills` 按需。
  prompt **必须英文六段**（CONTEXT / GOAL / STOP WHEN / EVIDENCE / DOWNSTREAM / REQUEST）。

#### 2.3 子代理契约（每个分析 task）

| 字段 | 要求 |
|---|---|
| GOAL | 对该 slug（或重叠组）产出 §3.1 **判定卡** + 裁决 `completed` \| `unfinished` \| `unknown` |
| MUST DO | 抽取 1–5 条产品交付目标；用代码/测试/docs/Git 至少一类外部证据核对；过程性 open 忽略；禁止 checkbox-first |
| MUST NOT | 删除或修改任何文件；改产品代码；改 `.omo` / `docs`；宣称全局清理完成；因 open 多/无 boulder 直接判 `unfinished` |
| EVIDENCE 返回 | 判定卡全文；关键现实证据路径或 commit；若 `unfinished` 写清具体缺口；若 `completed` 写清目标↔证据映射 |
| STOP WHEN | 该 task 范围内每个 slug 均有裁决 + 至少一条证据说明（`unknown` 须写缺什么） |

#### 2.4 父级在阶段 2 的职责

- 派发、等待、收集；同一 slug 失败则 **续用原 `task_id`** 补证据，不另开无关会话糊弄。
- **禁止**在全部应分析 slug 的裁决收回前：进入阶段 4、写 DELETE 清单、执行删除。
- **禁止**父级与子代理对**同一 plan** 并行重复搜同一证据目标（单一分析 owner）。

**阶段 2 出口物**：每个 plan slug 一份子代理（或合法父级自判）判定卡。缺任一强制分析 slug → 阶段 2 未闭合。

### 阶段 3 — 汇总裁决与「是否进入整理删除」门（MUST）

父协调者汇总阶段 2 结果：

1. 合成 `completed_slugs` / `unfinished_slugs` / `unknown_slugs`（冲突时：以**更具体的未完成外部证据**优先；证据不足则 `unknown`，不得猜 `completed` 去删）。
2. 结合阶段 1 项目列表，列出**可清理候选范围**（仅逻辑集合，尚未 dry-run）：
   - `completed` plan 及其 slug 附属（draft/notepad/evidence）
   - 未进入 `unfinished` 精确引用闭包的临时产物 / 根孤儿 / 已转换证据
3. **放行判定**：
   - **进入阶段 4（整理删除）**：存在至少一类可清理候选，或用户明确要求全量清理模式且未发现现实未完成阻断。
   - **不进入删除、直接收工**：无可清理候选，且用户未要求全量清理 → 输出项目列表 + 裁决表 +「无需删除」证明，**结束命令**。
4. 用户约束「只清已完成 / 未完成不要动」：用 `unfinished_slugs` 的**精确引用闭包**保护；**不得**把约束收窄成「只删同名附属」。

**阶段 3 出口物**：裁决表 + 放行/不放行声明 +（若放行）拟清理候选范围摘要。
**未放行 → 禁止阶段 4。**

### 阶段 4 — 整理删除（仅阶段 3 放行后）

仅在此阶段才允许：

1. 对**全部强制盘点桶**做文件级全量盘点（若阶段 1 仅为摘要，此处补全字面路径）
2. 按分类顺序 + 反向闭包逐项归入 KEEP / DELETE / UNKNOWN
3. 覆盖率门禁（§6）
4. **持久知识扫描 → 落地本地 `docs/`（§7）→ 核验无残留唯一事实**
5. 知识落地门禁通过后：dry-run 与执行集一致的字面路径删除
6. 证明结果（含知识落地表）

阶段 4 细节见「防遗漏硬门禁」「执行顺序」后半；**不得**在阶段 1–3 执行删除；**不得**跳过 §7 知识落地直接 dry-run。

### 编排反模式（失败执行）

| 反模式 | 正确 |
|---|---|
| 未出项目列表就对代码做完成度搜索并准备删除 | 先阶段 1 列表，再阶段 2 分析 |
| 父级独自扫完全部 plan 现实后直接 DELETE | ≥2 plan 必须子代理分析波 |
| 子代理还在跑就删「看起来完成」的 draft | 等阶段 2 闭合 + 阶段 3 放行 |
| 把「安排子代理分析」理解成用户要你**现在**清仓库 | 分析是命令内阶段；删除另属阶段 4 |
| 分析 task 里夹带删除/改代码 | 分析只读；删除仅父级在阶段 4 按清单执行 |
| 含架构/设计事实的 plan/notepad 未写入 `docs/` 就删 | 先 §7 落地本地 docs，再进 DELETE |
| 只写记忆、不写项目 `docs/`，宣称「知识已保留」 | 7.1 类事实 docs 优先；记忆不可替代 |
| 无脑清空 `.omo` / 按文件名批量删 | 逐项分类 + 覆盖率 + 知识落地表 |

## 防遗漏硬门禁

以下规则针对已验证的执行遗漏模式；与上文冲突时以本节为准（**编排阶段门禁优先**）。未通过阶段 3 放行与任一门禁前，禁止进入删除 dry-run。

### 1. 强制全量盘点桶（MUST）

在任何分类或删除前，必须对 `$WORKSPACE` 产出**完整字面路径清单**（文件级；目录仅作容器），至少覆盖下列桶，并报告每桶条目数：

| 桶 | 路径模式 | 说明 |
|---|---|---|
| plans | `$WORKSPACE/plans/*` | 每个 plan 文件 |
| drafts | `$WORKSPACE/drafts/*` | 每个 draft 文件 |
| notepads | `$WORKSPACE/notepads/**` | 每个 notepad 文件 |
| evidence 子目录 | `$WORKSPACE/evidence/*/` | 每个一级子目录及其内全部文件 |
| evidence 根文件 | `$WORKSPACE/evidence/*`（文件） | 根级 md/png/html/json 等 |
| 工作区根孤儿 | `$WORKSPACE` 根下不属于已知子目录的文件 | 如 visual-qa 日志、png |
| 状态线索 | `$WORKSPACE/boulder.json` 等 | **仅线索**，不作活跃根 |
| docs | `docs/**`（若存在） | 按 docs 流程另表盘点 |

可选桶（默认**不**因「已完成」自动清空，除非用户明确要求或全量清理模式）：`run-continuation/`、`start-work/`、`worktrees/` 注册表本身。

**完整性门禁**：`盘点总数 = KEEP 数 + DELETE 候选数 + UNKNOWN 数`。任一盘点路径未落入三集合之一 → **中止**，不得删除。

### 2. 用户约束解释（MUST）

当用户说「只清理已完成历史任务 / 未完成不要动」时：

- **允许**：删除已验证完成的 plan 及其附属，以及**不在**未完成活跃根精确引用闭包内的临时产物、已转换证据、根孤儿。
- **禁止**：把约束收窄为「只删与已物理删除的 plan **同 slug/同名** 的 notes/evidence」。
- **仍必须**：对全部强制桶做全量盘点与分类；未完成工作只通过**精确引用闭包**保护，不通过同前缀/同目录/「plan 文件还在」保护。

### 3. 计划完成度判定（MUST）— 项目现实优先，禁止 checkbox 首判

**核心原则**：plan 文本（checkbox、Status 字段、draft 状态、boulder）只是**线索**，不是裁决。裁决唯一依据是 **`.omo` 之外的项目现实**：代码、测试、配置、用户/维护者文档、Git 历史、issue tracker。
「open checkbox 很多」**不得**单独把 plan 标为未完成；「全部 `[x]`」也只是支持完成的线索，最终仍须核对现实（防误勾）。

#### 3.1 每个 plan 必填判定卡

| 字段 | 规则 |
|---|---|
| slug | `plans/<slug>.md` 文件名 |
| 目标摘要 | 从标题/Goal/验收标准提炼 **1–5 条可观察交付目标**（产品行为、API、文件迁移、文档 shipped 等）；忽略纯流程句 |
| open / done | 统计 `- [ ]` 与 `- [x]`（仅作线索） |
| 过程性 open | 仅 Momus/审批/handoff/「写进记忆」/纯流程说明、且无未交付产品目标 → **不**计入未完成 |
| 产品性 open | 指向具体未交付产品行为/文件/测试/接口的 open 项 → 进入现实核对清单 |
| 现实证据（完成） | 代码符号/路径、测试、配置、`docs/**` shipped 文档、相关 commit 等，证明目标已落地 |
| 现实证据（未完成） | 能指出**具体缺口**的外部证据（缺文件、行为未实现、测试仍失败、issue 仍 open） |
| boulder 线索 | `completed` 可支持可删；`active` **不得**单独证明未完成；**永不**作活跃根；**无 boulder 条目 ≠ 未完成** |
| 裁决 | `completed` / `unfinished` / `unknown`（见 3.2） |
| 禁止启发式 | 「plan 文件仍存在」「同名 evidence/draft/notepad」「有任意 open checkbox」「无 boulder 记录」均**不得**单独保留整包 |

#### 3.2 裁决顺序（MUST，禁止跳步；checkbox 不得先于现实）

对每个 plan：

1. **抽取交付目标**（3.1 目标摘要）。若几乎只有流程项、无产品目标 → 倾向 `completed`（执行壳），仍扫一眼是否有残留产品句。
2. **对每条产品目标做现实核对**（至少一类外部证据）：
   - 代码：关键符号/文件是否存在且行为匹配（grep/codegraph/读源码）
   - 测试：相关测试是否存在且近期可合理认为覆盖
   - 文档：`docs/**` / AGENTS / shipped 清单是否已描述该行为
   - Git：`git log` / 相关 commit message 是否表明已合并实现
3. **应用裁决**：
   - **全部**产品目标均有完成侧现实证据，且**找不到**具体未交付缺口 → 裁决 `completed`（**即使 open checkbox 很多、即使从未勾选、即使无 boulder**）
   - **至少一条**产品目标有**具体未交付**的外部证据 → 裁决 `unfinished`（进入活跃根候选；须在报告中写明缺口）
   - 目标模糊且现实证据冲突/不足 → 裁决 `unknown`（保留 plan 并报告；**不得**因 unknown 而扩大保留同前缀 evidence 堆）
4. **过程性 open 忽略**：裁决时视同不存在。
5. **重叠/替代 plan**：多个 plan 描述同一交付（如 `qr-create-retry-fallback` 与 `qr-create-serial-restart-fix`）时，**分别**对目标做现实核对；若同一落地已满足两者目标 → **两者均可 `completed`**，不得因「另一份 plan 还在」互相保护。

#### 3.3 完成侧快速信号（支持 `completed`，仍须对应到目标）

以下任一**且**能映射到该 plan 的交付目标时，记入「现实证据（完成）」：

- 关键实现文件/符号已存在，行为与 Goal/验收一致
- 维护者文档已写明该行为（如 `docs/**/shipped/**`、AGENTS 约束段）
- Git 历史中有明确实现该目标的 commit（message 或 diff 范围匹配）
- 迁移类：旧路径已删除且新 owner 包/入口已就位
- boulder `status=completed`（仅辅助，不可单独定案）

#### 3.4 未完成侧快速信号（支持 `unfinished`，须具体）

以下才可记入「现实证据（未完成）」：

- 计划要求的文件/导出/API **仍不存在**或行为明显不符
- 计划绑定的失败测试/验收命令在当前树仍失败（且失败点属于该 plan 范围）
- 用户/issue **明确**该目标尚未交付
- 活跃 worktree/分支**正在**实现该 plan 且 diff 显示目标未合入主工作区（须有路径/分支证据）

**不构成未完成证明**：open checkbox 文本、draft `ready-for-handoff`、evidence 目录存在、notepad 非空、无 boulder 条目、plan 文件修改时间新。

#### 3.5 循环保护与集合输出

- **禁止**：用「有 evidence 所以 plan 活跃」与「plan 还在所以 evidence 保留」互相证明。
- **顺序**：先按 3.2 独立裁决每个 plan → 得到 `completed_slugs` / `unfinished_slugs` / `unknown_slugs` → 再判定附属是否在**未完成**闭包内。
- **报告**：覆盖率报告必须列出每个 plan 的裁决 + 至少一条关键现实证据路径或 commit（`unknown` 须写清缺什么证据）。

### 4. 附属物与临时产物默认规则（MUST）

在 plan 完成度集合确定后：

1. **已完成 plan 的 slug 附属**（同 slug 的 draft、notepad 目录、evidence 子目录、明确 `plan_path` 指向该 plan 的 draft）→ 默认 **临时产物/已完成附属**，知识提取后删除；不得因 plan 文件尚未删而保留 draft。
2. **draft 状态**（`ready-for-handoff` / `approved` / `plan-ready` 等）只表示交接元数据，**不**保护已完成 plan 的 draft。
3. **evidence 中未进入未完成闭包的条目**（含 session 命名目录、visual QA 截图、根级 review md/png、HTML 报告）→ 默认 **临时产物**；若事实已在代码/测试/文档/Git 落地 → **已转换证据**，默认删原件。禁止未做临时产物判定就标未知。
4. **同名/同前缀/同级/同时段** 永不构成引用或保留理由。

### 5. 分类顺序（MUST，禁止跳步）

对每个盘点条目严格按序判定，命中即停：

1. 是否属于**项目现实证实**的未完成活跃根或其**精确引用闭包**？→ 保留
2. 是否为**已完成 plan** 或其余已完成附属？→ 知识提取后删除
3. 是否为**临时产物**（evidence/draft/notepad/截图/评审导出/执行日志/根孤儿媒体）？→ 知识提取后删除
4. 是否为**已转换证据**？→ 删除原件
5. 以上皆否 → **未知**，保留并报告

### 6. 删除前覆盖率报告（MUST）

dry-run 清单之前必须输出：

- 各强制桶盘点计数
- 每个 plan 的裁决表：`slug | 裁决 | open/done | 关键现实证据 | 未完成缺口(若有)`
- `completed_slugs` / `unfinished_slugs` / `unknown_slugs`（过程性残留须注明已忽略）
- KEEP / DELETE / UNKNOWN 计数且与盘点总数相等
- 明确声明：未使用「仅同名附属」捷径；未使用 boulder 作活跃根；**未使用 checkbox-first**；对 open 很多的 plan 已做现实核对

缺任一项 → 不得删除。

### 7. 删除前知识落地门禁（MUST，docs 优先）

**本命令不是无脑删除。** 对每一个 DELETE 候选源文件，删除前必须完成「持久事实扫描 → 去重 → 落地 → 核验」。

#### 7.1 必须落地到本地 `docs/` 的内容（面向人/维护者）

下列内容若仍准确、且未被现有 `docs/` / README / ADR **完整覆盖**，删除源文件前**必须**写入或更新项目本地文档（优先顺序见 7.3）：

| 类型 | 示例 |
|---|---|
| 架构 / 拓扑 | 宿主边界、包职责、运行时装配、目录所有权 |
| 设计决策 | 为何选 A 不选 B、约束与权衡、ADR 级结论 |
| 契约 / 接口 | 公共 API、IPC/HTTP 约定、错误语义、token 规则 |
| 验收 / 防回归 | 可复现验收标准、已知陷阱、禁止模式 |
| 运维 / 交接 | 仍有效的 runbook、调试入口、发布/验证命令锚点 |

**禁止**把上述内容只写进 Agent 记忆后就删源文件——记忆可作补充检索，**不能**作为架构/设计的唯一落点。

#### 7.2 默认可删、无需进 docs 的内容

- 执行日志、会话 ID、WIP checkbox 清单、阶段汇报、「做过什么」时间线
- 已转换的截图 / visual-QA / HTML 报告（事实已在代码或 docs）
- 纯过程性 draft 元数据（`ready-for-handoff` 等）
- 与代码/测试/已有 docs 完全重复的段落

#### 7.3 落地落点优先级（MUST）

1. **已有主题文档**：`docs/architecture/*`、`docs/process/*`、owner-local shipped、README、既有 ADR
2. **同主题扩展**：在现有目录下**更新**对应文件，禁止另起第二事实来源
3. **最小新建（允许）**：若确有 7.1 类唯一事实、且**没有任何**合适既有文件可挂靠，允许在 `docs/` 下创建**一个**主题明确的新文件（如 `docs/architecture/<topic>.md` 或项目已有 shipped 归口目录下的单一清单）；**禁止**新建整棵「推荐文档树」或 `docs/agent-dump/` 类垃圾桶
4. **记忆（仅补充）**：仅 `guardrails` / 短 `reference` 等 Agent 防回归提示可进记忆；且**不得**替代步骤 1–3 对 7.1 类事实的 docs 落地

全量清理模式的「文档先落地」与本条一致；普通清理**同样**适用 7.1–7.3，不得因「非全量」跳过 docs。

#### 7.4 硬阻断

对任一 DELETE 候选：

- 扫描出 7.1 类**独特**事实 → 必须先完成 7.3 写入/更新，并在处置清单记录 `docs 落点路径`
- 落点未写、写后未核对「源文件无新增唯一事实残留」→ **该路径禁止进入 dry-run DELETE 集**，改标 KEEP 或 UNKNOWN 并报告
- 不确定是否「独特 / 仍准确」→ **保留源文件**，不得猜测删除
- 覆盖率报告必须增加：`知识落地表`（源路径 | 事实摘要 | docs 落点或「无需/已覆盖」| 核验）

缺知识落地表或表中有未闭合行 → **禁止删除**。

## 分类定义

- **活跃根**：由 `.omo` 之外的项目现实证实、当前且未结的具体交付目标；Agent 状态文件本身不属于活跃根。完成或已替代的状态文件按普通条目分类，不自动种子化保留闭包。
- **引用依赖**：被活跃根或其递归依赖显式引用的条目。
- **已完成计划**：项目现实证明交付目标已落地（含纸面 checkbox 未勾、无 boulder 记录的情况），且不再被任何**未完成**活跃根或引用链需要的计划类内容。
- **临时产物**：证据、便签、草稿、截图、导出物、执行日志等一次性内容。
- **已转换证据**：其唯一事实已被转换进代码、测试、**项目 `docs/` / README / ADR**、Git 历史中的权威落点，原文件不再承载新增唯一事实。（便签/记忆可佐证「已转换」，但 7.1 类事实不能仅靠记忆宣称已转换。）
- **持久唯一知识**：内容中独有且值得长期保留的事实；**7.1 类必须迁移到本地 `docs/`（或 README/ADR）**，记忆仅作补充。
- **未知**：引用关系、状态或唯一性无法证明的条目。

## 目录探测

新版本 omo 已将工作目录从 `.sisyphus` 迁移到 `.omo`；同时，项目根目录的 `docs/` 可能混放用户文档与 Agent 执行产物。清理时需要把三者作为**并行候选目标**处理：

1. **探测存在性**：检查项目中 `.omo`、`.sisyphus` 和 `docs/` 是否存在
2. **确定工作区目标**：
   - 仅 `.omo` 存在 → 清理 `.omo`
   - 仅 `.sisyphus` 存在 → 以 `.omo` 为迁移目标进行清理（见下方迁移协议）
   - **两者都存在** → 先合并 `.sisyphus` 中的内容到 `.omo`，再清理两者（见下方迁移协议）
   - 两者都不存在 → 不设置 `$WORKSPACE`，但若 `docs/` 存在仍必须继续执行 docs 清理
3. **确定 docs 目标**：
   - `docs/` 存在 → 将 `docs/` 加入清理盘点范围
   - `docs/` 不存在 → 跳过 docs 清理
4. **结束条件**：仅当 `.omo`、`.sisyphus` 和 `docs/` 都不存在时，才报告无需清理并结束
5. 后续所有步骤中，将 `.omo` / `.sisyphus` 的目标目录简称为 `$WORKSPACE`；`docs/` 始终以 `docs/` 单独称呼

## 迁移协议

### 何时触发

- `.sisyphus` 和 `.omo` 同时存在 → 必须触发
- 仅 `.sisyphus` 存在，且盘点后发现**存在无法删除的活跃内容**（如未完成计划、活跃检查点） → 必须触发，将活跃内容迁移到 `.omo`

### 迁移流程

1. **盘点两者**：
   - 仅 `.omo` 存在时 → 盘点 `.omo`
   - 仅 `.sisyphus` 存在时 → 盘点 `.sisyphus`，识别活跃与可删除条目
   - 两者共存时 → 分别列出完整目录树，交叉对比

2. **分类 `.sisyphus` 内容**：
    - **活跃根**（由项目现实证实的未完成交付目标所需条目，不含 `boulder.json`） → 仅按精确引用闭包保留；若条目位于 `.sisyphus` 且仍被活跃根直接或递归引用，则迁移到 `.omo` 对应位置
    - **引用依赖** → 仅在属于精确引用闭包时迁移到 `.omo`
    - **已完成计划 / 临时产物** → 先走前置清单与知识提取，再删除
    - **持久唯一知识** → 先提取到既有权威文档或记忆，再删除原文件；不得新建文档树

3. **逐项合并策略**（两者共存时）：
   - 先对每个条目单独分类：活跃根、引用依赖、已完成计划、临时产物、持久唯一知识、未知
   - **活跃根及其精确显式依赖闭包** → 只迁移这些被精确引用的条目到 `.omo` 对应位置；目录本身仅作容器保留
   - **已完成计划 / 临时产物** → 必须先通过全局逐项处置清单，再在既有权威文档或记忆中完成持久唯一知识抽取后才可删除
   - **持久唯一知识** → 只迁移到已存在且明确的权威落点（README / ADR / 项目文档 / 记忆）；不新建文档树，不以“有价值”作为迁移理由
   - **未知** → 保留并报告，不得迁移或删除

4. **完成迁移后**：
   - 对 `.omo` 按正常流程执行清理
   - 删除 `.sisyphus` 中所有已迁移或已清理的内容
   - 若 `.sisyphus` 变空，移除 `.sisyphus` 根目录

## Docs 目录清理

### 概述

项目中的 `docs/` 目录常被 Agent 用于存放执行产物（计划文件、技术债追踪、架构分析等），与面向用户的持久文档混放。清理目标是：**删除已验证的 Agent 执行产物，保留所有面向用户的文档，在删除前把有价值的持久化知识提取到既有权威文档或记忆**。

### 探测条件

1. **探测存在性**：检查项目中是否存在 `docs/` 目录
2. **不存在** → 跳过 docs 清理，仅处理 `.omo` / `.sisyphus`
3. **存在** → 进入 docs 清理流程

### 内容分类

对 `docs/` 中每个文件/子目录进行分类：

#### 应保留（面向用户或维护者的持久文档）

- **帮助/使用文档**：用户手册、操作指南、FAQ、快速入门等
- **API/接口文档**：API 参考、接口规范、SDK 文档等开发者文档
- **项目说明/历史**：项目架构概述、CHANGELOG、版本历史、roadmap、设计决策记录（ADR）等
- **历史规划/项目记录**：已明确作为项目历史、产品演进或架构决策背景保留的规划文档
- **交接/运维文档**：handoff、runbook、debug skill、故障排查手册等仍能指导维护、调试或交接的文档
- **仍活跃的维护文档**：当前未解决且仍指导维护工作的技术债、风险清单、兼容性约束等
- **判断依据**：内容是面向项目使用者或维护者的持久参考，不因单个任务完成而过期；即使文件名包含 plan/debt/design/handoff，也不能仅凭文件名删除

#### 应清理（Agent 执行产物）

- **已完成计划文件**：实现计划、迁移计划、阶段计划、执行清单等，且相关工作已在代码/配置/文档现实中验证完成
- **已废弃计划文件**：路线已被替代、目标已取消，且不再作为项目历史或决策背景保留
- **已解决/过时技术债追踪**：TECH_DEBT.md、debt 清单、风险清单中记录的问题已修复、迁移到 issue tracker，或与当前代码现实不再相符
- **临时设计草案/分析**：架构分析、方案对比、临时调研文档、Agent 生成的方案评审文档，且长期价值已沉淀到 README、ADR、记忆或代码注释
- **临时证据/媒体产物**：截图、录屏、HTML 报告、导出 JSON、对比图等仅用于证明某次实现或评审的证据文件，且不再被用户文档、测试夹具、设计基线或发布材料引用
- **判断依据**：内容是 Agent 为完成特定任务而产出的临时文档，任务完成后价值已沉淀到代码、既有权威文档、记忆或其他持久载体；删除不会削弱用户帮助、API 参考、项目说明、历史规划、维护交接或排障手册的可用性

#### 未知

- 无法明确分类 → 保留并标记待后续处理

### Docs 保留标准

只有同时满足以下标准的 `docs/` 内容，才应作为保留对象：

1. **目的标准**：文档用于帮助用户使用项目、帮助开发者理解接口、帮助维护者理解当前架构/约束/历史决策，而不是只服务某一次 Agent 执行。
2. **受众标准**：目标读者是项目使用者、集成方、维护者或未来决策者；若目标读者只是当前任务执行者或当前会话，则不满足保留条件。
3. **时效标准**：内容在当前代码、配置、发布状态下仍准确；过期但具有历史价值的内容必须明确标注为历史记录、ADR、版本历史或路线演进，否则归为待清理/未知。
4. **唯一性标准**：文档承载的信息没有被 README、ADR、API 文档、CHANGELOG、issue tracker、代码注释或记忆完整覆盖；若完全重复且非用户入口文档，优先删除或合并。
5. **行动价值标准**：保留后能直接支持使用、集成、维护、排障、审计或理解项目演进；只记录“做过什么”的执行日志、阶段报告、临时结论不满足保留条件。
6. **责任边界标准**：文档是项目事实来源之一，或明确链接到事实来源；如果它会与代码现实、README、ADR 或 issue tracker 形成第二事实来源，应合并后删除。

快速判定：

| 问题 | 是 | 否 |
|---|---|---|
| 明确面向用户、集成方或维护者吗？ | 继续判断 | 倾向清理 |
| 当前仍准确，或已明确标注为历史/ADR/版本记录吗？ | 继续判断 | 倾向清理或未知 |
| 删除会损害使用、集成、维护、审计或理解项目历史吗？ | 保留 | 继续判断 |
| 内容是否只是某次任务的计划、证据、阶段汇报或临时分析？ | 倾向清理 | 继续判断 |
| 独特知识是否已经沉淀到 README/ADR/项目文档/记忆/issue？ | 可清理原文 | 先提取再判断 |

文件名只能作为线索，不能作为结论。`plan`、`debt`、`design` 文件若已被明确纳入历史规划、ADR 或活跃维护文档，可保留；`guide`、`api`、`overview` 文件若内容实际是临时执行日志，也应清理或改名归档后再保留。

### 参考路径样本规则

当 `docs/` 中出现类似 `analysis/`、`plans/`、`handoff/`、`reference/`、`skills/`、`superpowers/`、`screenshots/` 的目录时，按内容和状态处理：

- `reference/`、API 参考、架构说明、用户入口文档：若当前准确且被 README 或项目入口引用，默认保留；若过期，应更新或标注历史，而不是静默删除。
- `skills/`、runbook、debug 指南：若仍可指导维护者执行排障、调试或交接，默认保留；若只服务某次 Agent 操作且未被复用，按临时产物处理。
- `handoff/`：若记录的是可复用架构、接口、运维或迁移交接事实，保留或归档为项目历史；若只是一次会话的执行日志、提交清单或上下文转储，提取仍有价值的事实后删除原文。
- `analysis/`：若文档开头明确标注 Historical/ADR/当前适用范围，且能帮助理解历史决策或当前架构，保留；否则在结论已沉淀后按临时分析清理。
- `plans/`、`design/`：优先读取 `Status`、checkbox、日期和引用关系；`Draft`/未完成/仍指导实施的计划保留，`Superseded`/已完成/已废弃的计划仅在作为路线演进、ADR 或历史背景时保留，否则提取知识后清理。
- `screenshots/`、录屏、导出报告等媒体文件：只有在被用户文档、设计基线、测试夹具、发布说明或审计记录引用时保留；孤立的执行截图和阶段证据默认属于可清理候选。

若目录本身混有多类内容，不得整目录删除；先逐文件分类，再删除空目录。目录相邻、同级或同前缀不构成引用。

### Docs 最小归位原则

清理 `docs/` 时不创建新的推荐文档树，不做广义重组。只有在**既有权威落点已经明确存在**、**要保留的持久事实已经抽取完成**、且**受影响引用已验证**时，才允许进行最小移动或合并。

允许动作仅限：

- 将已抽取的持久唯一知识移动到现有 README / ADR / 项目文档 / 记忆所对应的既有落点；
- 将明显重复且已被确认的临时原文合并/折叠到既有权威来源；
- 仅为保留已验证事实而更新已存在的相对链接；
- 保留未知项，直到引用链和状态都被证明。

禁止动作：

- 新建文档树、目录模板或“推荐结构”；
- 以目录重组代替事实抽取；
- 因文件名、主题相似或目录相邻而批量搬迁；
- 未验证引用就移动、合并或删除文档。

### Docs 清理流程

1. **盘点**：列出 `docs/` 完整目录树，读取每个文件的内容概要
2. **分类**：按上述标准将每个条目分为「应保留」/「应清理」/「未知」
3. **验证状态**：
    - 对「应清理」条目：验证相关工作是否确实已完成（检查代码现实、记忆、issue tracker）
    - 对计划文件：确认计划项对应的代码、配置、测试、文档或发布现实是否已完成/废弃/迁移
    - 对技术债追踪文档：逐条检查问题状态；未解决且仍准确的条目应保留或归并到维护文档，已解决/失效的条目标记可清理
    - 对设计草案/分析：确认其结论是否已进入 README、ADR、架构文档、代码现实或记忆
    - 不确定时归类为「未知」
4. **持久化知识提取（§7）**：删除前扫描架构/设计/契约等 7.1 事实；**优先写入/更新项目 `docs/`**（及 README/ADR）；记忆仅补充 Agent 防回归，不得替代 docs
5. **最小归位**：落点优先既有主题文件；无挂靠点时允许 `docs/` 下**单一**主题文件最小新建（§7.3）；禁止按目录重组或建立新树/agent-dump
6. **删除已验证的执行产物**：仅在步骤 3–5 完成、知识落地表闭合、全局逐项处置清单通过后删除
7. **证明结果**：列出 `docs/` 剩余与**本次落地路径**；说明用户文档/API/架构/历史规划如何被保留或更新

### 全局逐项处置清单

在任何 workspace 或 docs 删除前，必须为**每个候选项**填写并核对以下字段；没有完成的候选项不得进入删除步骤：

| 字段 | 要求 |
|---|---|
| 路径 | 候选项的准确路径 |
| 分类 | 活跃根 / 引用依赖 / 已完成计划 / 临时产物 / 持久唯一知识 / 未知 |
| 证据 | 证明状态、完成度、或可废弃性的文件/命令结果 |
| 引用链 | 说明是否来自活跃根、直接引用、递归引用，或未被引用 |
| 持久知识去重/提取目标 | 现有 README / ADR / 项目文档 / 记忆中的既有落点；不得新建文档树 |
| 处置 | 保留 / 迁移 / 删除 / 未知 |
| 删除后验证 | 删除后的路径检查、剩余引用检查、目录空性检查 |

### 路径安全与 dry-run 门禁

1. **规范根只认 `.omo`**：清理的规范根始终是 `.omo`；`.sisyphus` 只在迁移协议里作为来源处理，不得把规范根切换到别的路径语义。
2. **只允许安全规范化**：规范化仅可解析分隔符和绝对路径；不得移除、替换、折叠或重命名任何路径段。若 `.omo/...` 规范化后变成 `omo/...`、丢失前导点、或任何片段发生变化，必须立刻中止。
3. **dry-run 与执行集必须完全一致**：两阶段必须在数量、排序后的字面路径、规范化后的绝对路径、分类、理由上逐项一致；任一差异都表示门禁失败，禁止执行删除。
4. **精确授权只认字面文件清单**：允许删除的目标只能来自已验证的字面路径列表；每项都必须记录原始字面路径、规范化绝对路径、期望父目录、分类与理由。不得做通配、前缀、兄弟目录或递归扩展。

未知项必须在清单中标记为保留并报告。

### Docs 删除检查清单

仅在以下**全部**为真时才删除 docs 中的文件：

- 内容已确认为 Agent 执行产物，非面向用户的持久文档
- 相关工作在代码/仓库中已验证完成（或已废弃/已迁移）
- 任何独特持久化知识已提取到既有权威文档或记忆，且无新增唯一事实残留
- 删除不损害项目使用者的文档可用性
- 清理后状态可立即验证

不确定时，保留并标记待后续处理。

## 加载条件

以下任一条件为真时加载：

- 用户要求清理、修剪、归档、重置或移除 `.sisyphus`、`.omo` 或 `docs/` 内容
- 目录包含可能已过期的计划、便签、证据、状态文件或空文件夹
- `docs/` 中混放了 Agent 执行产物和用户文档，需要区分清理
- 用户希望仅保留有用的架构笔记和用户文档，同时删除已完成的执行产物
- 清理决策取决于条目是否已完成、活跃、已在记忆中重复或可安全删除

## 强制规则

1. **阶段门禁**：必须按「编排与阶段门禁」0→1→2→3→（放行后）4 执行；禁止跳过项目列表或子代理分析波直接删除。
2. **阶段 1 父级只读项目列表**：先列出 plans/drafts/notepads/evidence/boulder 摘要；此阶段禁止深度现实裁决与任何删除。
3. **阶段 2 子代理分析**：`plans ≥ 2` 必须派只读子代理按 slug/重叠组做 §3 现实完成度判定；父级汇总，禁止父级独吞全部 plan 核对后跳过派发。
4. **阶段 3 放行**：合成 completed/unfinished/unknown 后才决定是否进入整理删除；未放行则报告后结束，不 dry-run。
5. 删除前对**强制全量盘点桶**做文件级清单（见「防遗漏硬门禁」）；禁止只扫已删 plan 的同名附属；禁止仅凭文件名分类。
6. 完成度判定遵守「防遗漏硬门禁 §3」（**项目现实优先，禁止 checkbox-first**）。open 很多或无 boulder 的 plan 必须经阶段 2 目标↔代码/文档/Git 核对后才能裁决。
7. 将计划文件、证据、笔记、草稿、技术债追踪、设计草案视为**候选产物**，直到由项目现实证明其属于**未完成**活跃根的精确引用闭包或可安全删除；`boulder.json` 仅作盘点线索，绝不作为活跃根；checkbox/draft 状态绝不单独定案。
8. **知识落地优先于删除（§7）**：删除含架构/设计/契约/拓扑/防回归/运维交接等独特事实的源文件前，必须先写入或更新项目本地 `docs/`（或 README/ADR）；不得以「只写了记忆」代替 docs。
9. 去重：`list_memories` + 既有 `docs/` / README / ADR；已覆盖则不再复制。禁止任务日志镜像到记忆或 docs。
10. 记忆仅作 Agent 补充（短 `guardrails` / `reference`）；**不得**成为 7.1 类事实的唯一载体。
11. 仅在唯一价值被证伪，或已作为**代码/测试/项目 docs/README/ADR/Git** 权威落点存在后，才删除临时产物；已转换证据默认删原件。**未入未完成闭包的 evidence/draft/根孤儿默认按临时产物处置**，不得跳步标未知。
12. 仅在确认真正为空后才移除空目录。
13. 以证据结束：剩余树 + 阶段出口物 + 覆盖率 + **知识落地表**。
14. **反向闭包**：凡盘点到且不在保留闭包内的路径，必须进入处置清单；禁止「未链接 ⇒ 跳过」。
15. **禁止循环保护**：plan 与 evidence/draft/notepad 不得互相作为唯一活跃证明。
16. **禁止无脑删除**：任一 DELETE 路径缺少处置记录中的「持久知识处理 / docs 落点或无需理由」→ 不得删。

## 分类检查

**验证后**对每个条目分类：

- **活跃根** — 仅由项目现实证实、仍有具体未交付目标的 plan（裁决 `unfinished`）→ 保留其精确引用闭包；plan 文本/boulder/checkbox 本身不是活跃根
- **引用依赖** — 被未完成活跃根直接或递归显式引用的条目 → 仅在闭包内保留
- **用户/维护者文档** — 帮助文档、API 文档、项目说明、历史规划、ADR、版本历史 → 保留
- **已完成计划** — 裁决 `completed`（现实目标已落地，含「纸面未勾但代码已落地」）→ 删除，前提是 §7 知识落地门禁通过
- **临时产物** — 证据输出、执行日志、过期追踪器、临时设计分析、草稿 → 删除，前提是 §7 通过（无 7.1 残留或已落地）
- **持久唯一知识** — 架构、设计、拓扑、契约、防回归、运维交接中独有部分 → **必须保留在本地 `docs/` / README / ADR**；记忆仅补充
- **未知** — 裁决 `unknown` 或走完分类顺序五步后仍无法判定 → 保留并报告；不得因 unknown plan 扩大同前缀清理豁免

## 执行顺序

对齐「编排与阶段门禁」。编号中的「阶段 N」与该节一致。

### 阶段 0–1（父级，只读）

1. **探测所有目标目录**：按「目录探测」确定 `$WORKSPACE` 与 `docs/`；需要时执行「迁移协议」准备。不删除。

2. **输出项目列表（阶段 1 出口）**：父级列出 plans（slug + open/done 粗计）、drafts、notepads、evidence 一级目录与根文件数、boulder 线索摘要、根孤儿文件名。**禁止**本步做深度代码现实裁决或 DELETE。

### 阶段 2（子代理分析波）

3. **按项目列表派只读分析子代理**：
    - `plans ≥ 2`：每个 slug（或重叠组）一个分析 task；并行建议 ≤3
    - `plans = 1`：默认 1 个分析 task（trivial 流程壳可父级自判并声明）
    - `plans = 0`：跳过本步
    - 每个 task 返回 §3.1 判定卡 + 裁决 + 现实证据；**禁止**子代理删除或改文件
    - 未收齐全部强制 slug 裁决 → **停止**，不得进入阶段 3 放行后的删除路径

### 阶段 3（父级汇总门）

4. **汇总裁决 + 放行判定**：
    - 合成 `completed_slugs` / `unfinished_slugs` / `unknown_slugs`
    - 输出裁决表与可清理候选范围摘要
    - **无可清理候选且非全量清理** → 报告后**结束命令**（不进入阶段 4）
    - **放行** → 继续步骤 5

### 阶段 4（仅放行后：分类 → 删除）

5. **强制全量盘点（文件级）**：按「防遗漏硬门禁 §1」补全字面路径并计数。**禁止**以「已完成 plan 列表」为起点只扫同名附属。建立直接/递归引用。

6. **按分类顺序逐项归类**：对盘点每一路径执行「防遗漏硬门禁 §5」。仅 `unfinished` 的**精确引用闭包**为 KEEP；`completed` 及其 slug 附属默认 DELETE 候选；其余临时产物/已转换证据进 DELETE；`unknown` 保留。覆盖率：`KEEP + DELETE + UNKNOWN = 盘点总数`。

7. **验证状态，而非推断**：
    - KEEP：须有具体未交付缺口的现实证据；禁止 boulder/checkbox/同名 evidence 循环证明
    - DELETE 中的 `completed`：须有阶段 2 判定卡中的完成侧证据映射
    - 其他 DELETE：已完成附属 / 临时产物 / 已转换证据；用户约束按「防遗漏硬门禁 §2」
    - `docs/`：Agent 产物是否已完成/已废弃

8. **删除前审查并落地持久化知识（§7 MUST）**：
    - 扫描 DELETE 候选中的架构/设计/契约/拓扑/防回归/运维事实
    - `list_memories` + 读既有 `docs/` / README / ADR 去重
    - 独特且仍准确的 7.1 类事实 → **写入/更新本地 `docs/`**（优先既有主题文件；必要时按 7.3 最小新建单一主题文件）
    - 输出 **知识落地表**；未闭合行对应路径不得进 DELETE
    - 禁止镜像任务日志；禁止「只写记忆不写 docs」宣称完成

9. **最小归位**（docs）：事实已抽取、引用已验证后，合并到既有权威落点；允许 7.3 的最小新建，禁止新建文档树或 agent-dump 目录。

10. **覆盖率报告 + 知识落地门禁 + dry-run**：输出 §6 与 §7.4；**知识落地表全部闭合**后才生成字面路径 DELETE 清单；dry-run ≡ 执行集。

11. **按清单删除**：仅删除核准字面路径；禁止前缀/兄弟扩展；空目录再移除。

12. **证明结果**：剩余树；已删/已留；覆盖率；**知识落地表与 docs 改动路径**；无 7.1 类知识丢失说明。

## 删除检查清单

仅在以下**全部**为真时才删除：

- 状态已验证，非假定
- 已完成全局逐项处置记录，且包含原始字面路径、规范化绝对路径、期望父目录、分类、证据、引用链、**持久知识处理（docs 落点路径或「无需/已覆盖」理由）**、处置、删除后验证
- **§7 知识落地门禁已通过**：知识落地表无未闭合行；7.1 类独特事实已在本地 `docs/` / README / ADR（不得仅记忆）
- 路径安全与 dry-run 门禁已通过，且 dry-run / execution 的集合在数量、排序后的字面路径、规范化绝对路径、分类、理由上完全一致
- 不在任何由项目现实证实的活跃根的精确引用闭包内；`boulder.json` 和其他 Agent 状态文件不得作为此项判断依据
- 不是面向用户或维护者的持久文档（`docs/` 中应保留类一律 KEEP）
- 源文件无新增唯一事实残留；已转换证据不再保留原件的第二份摘要
- 移除减少噪音而不产生第二事实来源
- 清理后状态可立即验证

不确定时，**保留**并标记待后续处理——**宁留不删**。

## 记忆处理协议

对拟删除源中的知识：

1. 先判断是否属 §7.1（架构/设计/契约等）→ **若是，先落地 `docs/`**，记忆步骤可选且不能替代
2. 再：`list_memories` → `read_memory` 检查重叠 → 决定（保留已有/更新已有/新建短 guardrail/不操作）
3. docs 与（可选）记忆均完成后，才允许该源路径进入 DELETE

**禁止存储到记忆**：会话 ID、一次性执行日志、临时审计笔记、验证输出文本、大量复制的计划历史、**完整架构专章（应去 docs）**

## 报告结构

按阶段汇报（缺阶段出口视为失败执行）：

1. **阶段 1 项目列表**（各桶计数 + plan 行表）
2. **阶段 2 分析**（每个 slug 的裁决 + 关键现实证据；子代理 session 可附）
3. **阶段 3 放行声明**（进入删除 / 无需删除收工）
4. 若进入阶段 4：覆盖率 → **知识落地表（源 | 事实 | docs 落点）** → dry-run → 已删除 → **本次更新的 docs 路径列表** → 最终证据（`剩余目录树` 或 `路径不存在`）

## 验证案例

- **混合活跃/已完成 `.omo`**：仅保留被项目现实证实的活跃根精确引用的条目；已完成部分若不在引用闭包内，必须进入逐项处置清单并删除；`boulder.json` 不构成活跃证据。
- **明确全量清理 `.omo`**：若项目现实未证明任何未完成计划，先将仍准确且有用的文档事实落入 `docs/`，再删除指定 `.omo` 根目录；不得因 `boulder.json`、Agent 日志或 `.omo` 内未核验计划而保留该目录。
- **已完成计划中的唯一持久事实**：先抽取到本地 `docs/` / README / ADR（§7），再删除原计划；仅写记忆不算完成。
- **已转换证据默认删除**：证据若已无新增唯一事实，且持久事实已在代码、测试、**项目 docs**、Git 落地，则删除原件，不再生成第二份摘要。
- **无脑删除失败**：未输出知识落地表、或表中有未闭合 7.1 事实仍执行删除 → 失败执行。
- **架构只进记忆失败**：plan/notepad 含独特架构/设计事实，仅 `write_memory` 后删源、未更新 `docs/` → 失败执行。
- **未知条目**：仅在走完分类顺序五步后仍无法判定时保留并报告；禁止把未链接 evidence 直接标未知。
- **docs 文件名/内容陷阱**：文件名像 `plan`、`handoff`、`reference` 但内容是临时产物时，按内容与引用链判定，不按名称保留。
- **精确 transitive dependency vs 兄弟目录**：仅保留被直接引用并递归可达的依赖；同级兄弟目录、邻近目录、同前缀目录一律不算引用。
- **`.omo` / `omo` 分离**：若规范化导致 `.omo/...` 变成 `omo/...`、丢失前导点或改写任意片段，立即中止，不得继续清理。
- **dry-run / execution 完全一致**：若执行集与 dry-run 在数量、字面路径、规范化路径、分类或理由上不一致，立即中止，不得删除任何项。
- **用户约束收窄陷阱**：用户说「只清已完成」时，仍必须全量盘点 evidence/drafts/根孤儿；不得只删与已删 plan 同 slug 的附属而留下无关 visual-QA 堆。
- **过程性 checkbox 陷阱**：plan 仅剩 Momus/审批/handoff 类 open 项且无未交付产品目标时，不得因此保留 plan/draft/evidence 整包。
- **checkbox-first / 纸面未勾陷阱**：plan 有大量 `- [ ]` 且无 boulder，但代码/测试/shipped 文档/Git 已证明 Goal 落地（如 QR create 重试、effect 解耦）→ 必须裁决 `completed` 并进入 DELETE，不得因「全是 open」保留。
- **无 boulder ≠ 未完成**：boulder 中无对应 work 条目时，仍须做现实核对；不得默认 KEEP。
- **重叠 plan 同交付**：两个 plan 描述同一已落地行为时，两者均可 `completed`，不得互相作为未完成证明。
- **draft 与 plan 生命周期**：plan 已完成但 draft 仍为 `ready-for-handoff`/`approved` 时，draft 必须进入 DELETE 候选，不得因 draft 状态或 plan 文件仍在而保留。
- **循环保护陷阱**：不得因存在 `evidence/<slug>/` 而将已全部 `[x]` 或已现实完成的 plan 标为活跃，再反向保留该 evidence。
- **覆盖率门禁**：若 `KEEP+DELETE+UNKNOWN ≠ 盘点总数`，中止删除并补盘点；首轮只处置 4 个 plan 附属而 evidence 桶未分类，视为失败执行；覆盖率报告缺 plan 裁决表或缺现实证据列，视为失败执行。
- **跳过项目列表**：未输出阶段 1 项目列表就进入代码现实搜索或删除 → 失败执行。
- **跳过子代理分析波**：`plans ≥ 2` 时父级独自核对完全部 plan 后直接 DELETE，或未收齐判定卡就 dry-run → 失败执行。
- **分析未放行就删**：阶段 3 未声明放行（或声明无需删除）却执行删除 → 失败执行。
- **分析 task 写文件**：阶段 2 子代理删除/修改 `.omo` 或产品代码 → 失败执行；删除仅父级在阶段 4 按字面清单执行。

**边界**：使用 `/restructure-memory` 处理跨多条记忆的存储级重组；使用 `/cleanup-sisyphus` 处理 `.sisyphus` / `.omo` 执行产物清理和 `docs/` Agent 产物清理。当清理发现持久化知识缺口时，使用 `/restructure-memory` 原则进行保留步骤。
